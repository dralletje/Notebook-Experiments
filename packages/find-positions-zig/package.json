{
  "name": "find-positions-zig",
  "version": "1.0.0",
  "description": "",
  "main": "find-positions-zig.js",
  "scripts": {
    "build": "zig build-lib main.zig -target wasm32-freestanding -dynamic -OReleaseSafe",
    "build-fast": "zig build-lib main.zig -target wasm32-freestanding -dynamic -OReleaseFast",
    "test": "echo \"Error: no test specified\" && exit 1",
    "which-zig": "which zig"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@zig-on-npm/zig": "^0.10.0-rc.2"
  }
}
